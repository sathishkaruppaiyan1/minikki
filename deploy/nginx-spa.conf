# Nginx: SPA fallback so refresh on /about-us, /product/123, etc. works.
# Add this location block inside your server { } for the site.
# Replace /var/www/html with the path to your dist folder.

location / {
  root /var/www/html;
  try_files $uri $uri/ /index.html;
}
